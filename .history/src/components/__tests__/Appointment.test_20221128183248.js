import React from "react";

import { render, cleanup } from "@testing-library/react";

import Appointment

afterEach(cleanup);

it("renders without crashing", () => {
  render(<Application />);
});


